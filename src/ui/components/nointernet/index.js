import './style.css';

const template = `
    <div class="loading-wrapper">
        <p>You're not connected to the network...</p>
    </div>
`;

const renderLoading = () => template;

export default renderLoading;
